/** @type {import('next').NextConfig} */
const nextConfig = {
  // API-only app — no pages, no image optimisation, no client bundle to speak of.
  reactStrictMode: true,
};

export default nextConfig;
